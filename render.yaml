services:
  - type: web
    name: baicai-cup-random-hero
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PLAYER_PASSWORD
        value: "123456"
      - key: ADMIN_PASSWORD
        value: "baicai2024"
