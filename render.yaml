services:
  - type: web
    name: baicai-cup-random-hero
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PLAYER_PASSWORD
        value: "123456"
      - key: ADMIN_PASSWORD
        value: "baicai2024"
      - key: SESSION_SECRET
        value: "baicai-cup-v2-secret-key"
